@model Maritimo.Web.ViewModels.AsignarPermisoVM


@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        @TempData["Error"]
        <button class="btn btn-sm btn-outline-dark"
                onclick="this.closest('.alert').remove()">
            ✖
        </button>
    </div>
}



<h3>Asignar Permisos</h3>
<div class="card">

    <div class="card-body">

        <p><strong>Rol:</strong> @Model.NombreRol</p>

        @using (Html.BeginForm())
        {
            @Html.HiddenFor(m => m.IdRol)

            <div class="form-group mb-3">

                <label>Cursos</label>

                <div class="form-group">

                    @foreach (var permiso in Model.Permisos)
                    {
                        <div class="form-check">

                            <input class="form-check-input"
                                   type="checkbox"
                                   name="PermisosSeleccionados"
                                   value="@permiso.Value" />

                            <label class="form-check-label">
                                @permiso.Text
                            </label>

                        </div>
                    }

                </div>

                <button type="submit" class="btn btn-success">
                    Asignar
                </button>

                <a href="@Url.Action("Index", "Rols")" class="btn btn-secondary">
                    Cancelar
                </a>

            </div>

        }
    </div>
</div>
